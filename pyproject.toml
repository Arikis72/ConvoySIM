[project]
name = "convoysim"
version = "0.1.0"
description = "Three-truck convoy simulation foundation"
requires-python = ">=3.11"
dependencies = ["Pillow"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
