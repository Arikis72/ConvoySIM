# Nested repositories — SimGenerator is a separate git repo inside this directory
SimGenerator/

# AI session files (Claude Code memory)
.claude/

# Python
__pycache__/
*.py[cod]
*$py.class
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/

# ConvoySIM — generated outputs (large, transient, not source)
outputs/

# ConvoySIM — user-specific settings
convoysim.ini

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
*.swp
*.swo

# Scratch / personal working files
ChatGPT.txt
ConvoySimKey
ConvoySimKey.pub

# Personal notes — never commit passwords, tokens, or secrets
myNotes.md
myNotes.md
*.env
*.env.local
.env*

# Test artifacts
.coverage
htmlcov/
